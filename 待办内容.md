# Zhengyan.MikuMikuDance 待办内容

本文档用于记录相对 nanoem 完整功能集仍待实现的内容。后续每完成一项功能，需要同步更新对应条目的状态、说明或拆分子任务。

状态约定：

- `[ ]` 未开始或未完整实现
- `[-]` 已有基础实现，但仍需扩展到 nanoem 级别
- `[x]` 已完成当前目标范围

## 已具备基础能力

- `[x]` .NET 10 解决方案与 `Zhengyan.MikuMikuDance.*` 命名空间工程结构
- `[x]` PMD/PMX 基础模型读取与写入
- `[x]` VMD/NMD 基础 motion 读取与写入
- `[x]` `.zmm`/`.nma` 项目读取与写入
- `[x]` PMMv1/PMMv2 基础导入与 PMMv2 导出
- `[x]` ASCII DirectX `.x` 附件读取与写入
- `[x]` MME `.fx` 结构读取、pass state、基础 execution plan 和基础 GLSL 翻译
- `[x]` Silk.NET OpenGL/GLFW 基础预览窗口
- `[x]` PMD/PMX/`.x` 基础 OpenGL 渲染、纹理、toon、sphere、edge、ground shadow
- `[x]` VMD 采样、CPU skinning、Morph、CCD IK、相机/灯光/可见性应用
- `[x]` MME render target/offscreen prepass 与外部 default-effect 初步加载
- `[x]` 基础 motion timeline 编辑原语
- `[x]` CLI inspect/preview/export-pmm/pose 命令

## 第二阶段：编辑器壳与命令系统

- `[ ]` 新增 ImGui.NET 编辑器 UI 项目，例如 `Zhengyan.MikuMikuDance.UI.ImGui`
- `[ ]` 将 ImGui.NET 接入 Silk.NET OpenGL/GLFW host
- `[ ]` 实现主菜单、工具栏、dock layout、状态栏
- `[ ]` 实现项目/场景对象面板
- `[ ]` 实现时间轴面板和帧范围/缩放/滚动交互
- `[ ]` 实现播放控制面板
- `[ ]` 实现模型、附件、相机、灯光、Morph 基础参数面板
- `[ ]` 实现偏好设置面板和偏好持久化
- `[x]` 完整 undo/redo command 框架：stack 状态、delegate、batch、snapshot 命令基础
- `[x]` 添加/删除/更新模型关键帧命令
- `[x]` 添加/删除/更新骨骼关键帧命令
- `[x]` 添加/删除/更新 Morph 关键帧命令
- `[x]` 添加/删除/更新相机关键帧命令
- `[x]` 添加/删除/更新灯光关键帧命令
- `[x]` 添加/删除/更新附件关键帧命令
- `[x]` 添加/删除/更新 self-shadow 关键帧命令
- `[x]` 批量 undo command 与 motion/model snapshot command
- `[x]` 插入/删除 timeline frame 命令
- `[x]` 复制、粘贴、镜像粘贴关键帧

## 第三阶段：视口交互与基础编辑

- `[ ]` 视口对象 picking/selection
- `[ ]` 模型、附件、骨骼、Morph 当前活动对象选择器
- `[ ]` 变换 gizmo：移动、旋转、缩放
- `[ ]` 骨骼拖拽编辑
- `[ ]` 相机拖拽/轨道/平移/缩放控制
- `[ ]` Morph 拖拽或滑块编辑
- `[ ]` 附件外部亲子/模型骨骼绑定 UI
- `[ ]` 相机外部亲子/模型骨骼绑定 UI
- `[ ]` draw order 编辑
- `[ ]` transform order 编辑
- `[ ]` model outside parent 编辑
- `[ ]` 当前选择高亮、选择描边或 debug overlay
- `[ ]` 快捷键与菜单命令路由

## 第四阶段：渲染与 MME 深化

- `[ ]` 内置 shader/resource bundle 体系
- `[ ]` 内置 toon 资源打包与默认 toon 解析
- `[ ]` grid/debug/pointed debug 绘制
- `[ ]` selection/pointed model 绘制 pass
- `[ ]` self-shadow shadow map 渲染
- `[ ]` 背景图片渲染
- `[ ]` 背景视频渲染接口
- `[ ]` GPU skinning 或 transform feedback skinning
- `[ ]` 更完整透明排序与材质渲染细节
- `[ ]` 色彩转换/color transform pass
- `[ ]` OpenGL capture/capturing pass state
- `[-]` MME HLSL/FX 翻译器扩展：宏、include、struct、更多表达式和函数
- `[-]` MME 全局变量和语义绑定扩展：时间、鼠标、控制对象、材质颜色、灯光等
- `[-]` MME ScriptExternal 与外部 effect 交互规则补全
- `[ ]` MME animated texture 支持
- `[ ]` MME mipmap generation 支持
- `[ ]` MME shared render-target ownership 和生命周期管理
- `[ ]` MME effect 参数 UI 和运行时参数覆盖
- `[ ]` MME effect reload/diagnostics

## 第五阶段：物理系统

- `[ ]` 选择并接入跨平台物理库
- `[ ]` PMD/PMX rigid body 创建与同步
- `[ ]` PMD/PMX joint 创建与同步
- `[ ]` 物理世界 step、重置、暂停和播放同步
- `[ ]` 物理结果回写骨骼 pose
- `[ ]` 物理模拟设置面板
- `[ ]` 刚体/关节 debug 绘制
- `[ ]` soft body 支持

## 第六阶段：模型编辑器

- `[ ]` 模型对象选择系统
- `[ ]` 顶点编辑
- `[ ]` 材质编辑
- `[ ]` 骨骼编辑
- `[ ]` IK/constraint 编辑
- `[ ]` Morph 编辑
- `[ ]` Label 编辑
- `[ ]` 刚体编辑
- `[ ]` Joint 编辑
- `[ ]` SoftBody 编辑
- `[ ]` UV 编辑器
- `[ ]` 顶点权重绘制
- `[ ]` bind pose 管理
- `[ ]` 模型验证器
- `[ ]` 模型导入/导出编辑流程
- `[ ]` PMD/PMX 写回兼容测试扩展

## 第七阶段：音频、视频与导出

- `[ ]` 跨平台音频播放
- `[ ]` 音频和 timeline 同步
- `[ ]` 音频波形或播放状态 UI
- `[ ]` 背景视频解码
- `[ ]` 图片导出
- `[ ]` 视频录制
- `[ ]` FFmpeg 或平台编码器适配
- `[ ]` 导出进度、取消和错误报告

## 第八阶段：插件宿主

- `[ ]` 插件基础接口与生命周期
- `[ ]` Decoder plugin
- `[ ]` Encoder plugin
- `[ ]` ModelIO plugin
- `[ ]` MotionIO plugin
- `[ ]` Effect plugin
- `[ ]` MeshSync plugin
- `[ ]` 插件发现、加载、卸载
- `[ ]` 插件 UI 和参数面板
- `[ ]` 插件错误隔离和诊断
- `[ ]` 跨平台 native/plugin 调用封装

## 第九阶段：工程资源、平台和发布

- `[ ]` 字体资源 bundle
- `[ ]` 翻译/本地化资源
- `[ ]` 图标和应用资源
- `[ ]` 最近文件、窗口布局、用户偏好持久化
- `[ ]` Windows 平台菜单、文件对话框和打包
- `[ ]` Linux 平台菜单、文件对话框和打包
- `[ ]` macOS 平台菜单、文件对话框、Info.plist 和打包
- `[ ]` 文件关联
- `[ ]` 崩溃/错误报告和日志系统
- `[ ]` 集成测试与跨平台冒烟测试

## 格式兼容补充

- `[ ]` binary DirectX `.x` 支持
- `[ ]` compressed DirectX `.x` 支持
- `[-]` PMM/ZMM/NMA 项目字段补全：effect 绑定、插件状态、UI 状态、资源路径细节
- `[-]` PMD/PMX/VMD/NMD 边界样本和兼容测试扩展
- `[ ]` nanoem reference fixture 对照测试集

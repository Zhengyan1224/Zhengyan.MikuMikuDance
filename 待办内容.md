# Zhengyan.MikuMikuDance 待办内容

本文档用于记录相对 nanoem 完整功能集仍待实现的内容。后续每完成一项功能，需要同步更新对应条目的状态、说明或拆分子任务。

状态约定：

- `[ ]` 未开始或未完整实现
- `[-]` 已有基础实现，但仍需扩展到 nanoem 级别
- `[x]` 已完成当前目标范围

## 已具备基础能力

- `[x]` .NET 10 解决方案与 `Zhengyan.MikuMikuDance.*` 命名空间工程结构
- `[x]` PMD/PMX 基础模型读取与写入
- `[x]` VMD/NMD 基础 motion 读取与写入
- `[x]` `.zmm`/`.nma` 项目读取与写入
- `[x]` PMMv1/PMMv2 基础导入与 PMMv2 导出
- `[x]` ASCII DirectX `.x` 附件读取与写入
- `[x]` MME `.fx` 结构读取、pass state、基础 execution plan 和基础 GLSL 翻译
- `[x]` Silk.NET OpenGL/GLFW 基础预览窗口
- `[x]` PMD/PMX/`.x` 基础 OpenGL 渲染、纹理、toon、sphere、edge、ground shadow
- `[x]` VMD 采样、CPU skinning、Morph、CCD IK、相机/灯光/可见性应用
- `[x]` MME render target/offscreen prepass 与外部 default-effect 初步加载
- `[x]` 基础 motion timeline 编辑原语
- `[x]` CLI inspect/preview/export-pmm/export-image/pose 命令

## 第二阶段：编辑器壳与命令系统

- `[x]` 新增 ImGui.NET 编辑器 UI 项目，例如 `Zhengyan.MikuMikuDance.UI.ImGui`
- `[x]` 将 ImGui.NET 接入 Silk.NET OpenGL/GLFW host
- `[-]` 实现主菜单、工具栏、dock layout、状态栏：已完成基础 shell、dockspace、菜单和状态栏，后续接入真实命令路由与布局持久化
- `[-]` 实现项目/场景对象面板：已显示模型、附件和 motion 列表，后续补选择、层级和资源操作
- `[-]` 实现时间轴面板和帧范围/缩放/滚动交互：已支持当前帧滑块和基础帧信息，后续补缩放、滚动和关键帧轨道
- `[-]` 实现播放控制面板：已支持播放状态切换、前后帧和 loop 开关，后续接入 motion 播放时钟
- `[-]` 实现模型、附件、相机、灯光、Morph 基础参数面板：已接入相机和灯光参数，后续补模型、附件和 Morph 当前对象参数
- `[x]` 实现偏好设置面板和偏好持久化：已支持主题、清屏色、VSync、面板显隐和窗口尺寸保存
- `[x]` 完整 undo/redo command 框架：stack 状态、delegate、batch、snapshot 命令基础
- `[x]` 添加/删除/更新模型关键帧命令
- `[x]` 添加/删除/更新骨骼关键帧命令
- `[x]` 添加/删除/更新 Morph 关键帧命令
- `[x]` 添加/删除/更新相机关键帧命令
- `[x]` 添加/删除/更新灯光关键帧命令
- `[x]` 添加/删除/更新附件关键帧命令
- `[x]` 添加/删除/更新 self-shadow 关键帧命令
- `[x]` 批量 undo command 与 motion/model snapshot command
- `[x]` 插入/删除 timeline frame 命令
- `[x]` 复制、粘贴、镜像粘贴关键帧

## 第三阶段：视口交互与基础编辑

- `[-]` 视口对象 picking/selection：已实现渲染中立 CPU ray/triangle 拾取，UI 视口可选择模型和附件，后续补骨骼、Morph、深度缓冲拾取和高亮 pass
- `[-]` 模型、附件、骨骼、Morph 当前活动对象选择器：已支持模型/附件选择、模型内骨骼和 Morph 子选择，并与参数面板联动显示活动骨骼信息和活动 Morph 权重；后续补视口骨骼拾取、Morph 分组筛选、键盘导航和真实编辑命令接入
- `[ ]` 变换 gizmo：移动、旋转、缩放
- `[ ]` 骨骼拖拽编辑
- `[-]` 相机拖拽/轨道/平移/缩放控制：已支持视口右键轨道、中键平移、滚轮缩放和菜单重置，后续补真实渲染视口中的导航指示、快捷键配置和更完整的 nanoem 操作模式
- `[-]` Morph 拖拽或滑块编辑：已支持选中模型实例的 Morph 权重滑块、重置、网格构建应用和项目 JSON 持久化，后续补关键帧写入、曲线编辑、拖拽手柄和分组/类别筛选
- `[-]` 附件外部亲子/模型骨骼绑定 UI：已支持参数面板中为附件选择父模型和父骨骼、清除绑定，并复用 Core 层绑定校验；后续补关键帧写入、绑定后世界矩阵求解和 nanoem 外部亲子完整状态
- `[-]` 相机外部亲子/模型骨骼绑定 UI：已支持场景相机父模型/父骨骼状态、参数面板绑定/清除、JSON 持久化和 PMM 当前相机父绑定读写；后续补相机关键帧父绑定、绑定后视图矩阵求解和 nanoem 外部亲子完整状态
- `[-]` draw order 编辑：已支持 Scene 面板中模型和附件上移/下移，并同步影响项目列表顺序、JSON/PMM 写出顺序和当前选择索引；后续补拖拽排序、独立 transform order、关键帧化和 nanoem 完整 draw order 面板
- `[-]` transform order 编辑：已支持模型实例独立 transform order、Scene 面板中单独上移/下移、JSON/PMM 读写分离 draw order 与 transform order；后续补拖拽排序、关键帧化、外部亲子求解顺序和 nanoem 完整排序面板
- `[-]` model outside parent 编辑：已支持模型实例 outside parent 状态、参数面板中为带 outside-parent 标记的骨骼选择父模型/父骨骼、清除绑定、JSON 持久化，以及 PMM v2 outside-parent subject 骨骼标记导入；后续补关键帧 outside parent 状态、绑定后骨骼矩阵求解和 nanoem 完整 outside parent 面板
- `[-]` 当前选择高亮、选择描边或 debug overlay：已支持渲染中立的选中目标屏幕 overlay 计算，并在 ImGui 视口绘制选中矩形和名称；后续补真实 OpenGL selection pass、深度感知描边、骨骼/Morph 高亮和可切换 debug overlay
- `[-]` 快捷键与菜单命令路由：已新增 `EditorCommandRouter`，统一菜单、工具栏和基础快捷键入口，支持播放切换、前后帧、相机重置、面板显隐和占位文件/编辑命令路由；后续补真实 undo/redo stack 接入、文件对话框、剪贴板编辑命令和可配置快捷键

## 第四阶段：渲染与 MME 深化

- `[ ]` 内置 shader/resource bundle 体系
- `[-]` 内置 toon 资源打包与默认 toon 解析：已支持渲染中立 `@builtin/toonNN` shared toon URI、空 shared toon slot 的默认解析、`toonNN.bmp` 兼容识别，以及 OpenGL texture cache 按需生成内置 toon RGBA 纹理；后续补 nanoem 原始 toon PNG 精确打包、项目归档资源索引、贴图验证诊断和用户可替换默认 toon bundle
- `[-]` grid/debug/pointed debug 绘制：已支持渲染中立视口网格线段生成、ImGui 视口网格 overlay、鼠标 pointed debug 文本和偏好开关持久化；后续补 OpenGL debug pass、骨骼/刚体/关节 debug 绘制、深度感知 pointed overlay 和可配置网格参数
- `[-]` selection/pointed model 绘制 pass：已支持渲染中立 selected/pointed overlay role 和样式、ImGui 视口选中对象矩形与鼠标 pointed 对象矩形绘制；后续补真实 OpenGL selection/pointed pass、深度感知描边、骨骼/Morph pointed 绘制和可配置 overlay 样式
- `[ ]` self-shadow shadow map 渲染
- `[-]` 背景图片渲染：已支持场景背景图片状态、参数面板路径/启用/偏移/缩放/透明度/fit-fill 布局编辑、JSON/PMM 背景图片读写、ImGui 视口背景占位层，以及 OpenGL 背景图片 pass 的真实纹理加载、file URI/相对路径解析、纵横比适配、fill 裁切、缩放/偏移和透明度混合；后续补编辑器真实视口贴图合成、图片诊断提示和背景图片关键帧化
- `[-]` 背景视频渲染接口：已支持场景背景视频状态、参数面板路径/启用/偏移/缩放编辑、JSON/PMM 背景视频读写和 ImGui 视口视频占位层；后续补跨平台视频解码、帧上传纹理、音视频/timeline 同步和 OpenGL 背景视频 pass
- `[ ]` GPU skinning 或 transform feedback skinning
- `[-]` 更完整透明排序与材质渲染细节：已支持 `RenderBatchOrderingPlan` 将 opaque/transparent batch 分离、按视图深度稳定 back-to-front 排序、RGBA 贴图 alpha 扫描判定贴图透明候选，并让 OpenGL 主 pass 复用统一排序计划；后续补 alpha test/材质 draw flags、per-triangle 排序、OIT/depth peeling、双面透明材质和更完整 MME 材质状态
- `[-]` 色彩转换/color transform pass：已支持场景 brightness/contrast/saturation/gamma 状态、渲染中立颜色转换计算、JSON 持久化、参数面板编辑和基础 OpenGL 模型 shader uniform；后续补完整 framebuffer post-process pass、effect/offscreen/buffer pass 统一色彩转换、LUT/色调映射和可关键帧化
- `[-]` OpenGL capture/capturing pass state：已支持场景帧捕获请求/结果状态、OpenGL host 渲染后 `ReadPixels` RGBA 捕获、OpenGL bottom-left 到 top-left 行转换、捕获 buffer 校验、PNG 编码/写出和首帧捕获后自动关闭的 one-shot 导出流程；后续补 effect/offscreen target 捕获、异步 readback/PBO 和编辑器捕获 UI
- `[-]` MME HLSL/FX 翻译器扩展：宏、include、struct、更多表达式和函数
- `[-]` MME 全局变量和语义绑定扩展：时间、鼠标、控制对象、材质颜色、灯光等
- `[-]` MME ScriptExternal 与外部 effect 交互规则补全
- `[ ]` MME animated texture 支持
- `[-]` MME mipmap generation 支持：已支持 `.fx` 中 `Mipmap`/`MipMap`/`GenerateMipmap` annotation 元数据解析、sampler texture source 的 mipmap 标志传播，以及 OpenGL 外部贴图和 render target/offscreen texture 的 mipmap filter 与按需 `GenerateMipmap`；后续补 sampler state 过滤规则、动态纹理 mip 更新策略和更完整的 MME 兼容行为
- `[ ]` MME shared render-target ownership 和生命周期管理
- `[-]` MME effect 参数 UI 和运行时参数覆盖：已支持模型/附件级 bool/int/float/vector4 参数覆盖集合、JSON 持久化、RenderMesh 快照传递、OpenGL effect uniform 按名称覆盖，以及 ImGui 参数面板基础增删改；后续补 effect 文件绑定 UI、按 `.fx` annotation 自动生成控件、纹理/矩阵等更多参数类型、关键帧化和 undo/redo 命令接入
- `[-]` MME effect reload/diagnostics：已支持 OpenGL effect source cache 的显式 reload/invalidate/clear、缺失/隐藏/加载异常诊断记录，以及渲染中立 `RenderEffectDiagnostics` 对已编译 effect 的基础结构分析；后续补编辑器 diagnostics 面板、shader compile/link log 暴露、effect 文件热重载监听、外部 default-effect 依赖链刷新和用户可见错误定位

## 第五阶段：物理系统

- `[ ]` 选择并接入跨平台物理库
- `[ ]` PMD/PMX rigid body 创建与同步
- `[ ]` PMD/PMX joint 创建与同步
- `[ ]` 物理世界 step、重置、暂停和播放同步
- `[ ]` 物理结果回写骨骼 pose
- `[ ]` 物理模拟设置面板
- `[ ]` 刚体/关节 debug 绘制
- `[ ]` soft body 支持

## 第六阶段：模型编辑器

- `[ ]` 模型对象选择系统
- `[ ]` 顶点编辑
- `[ ]` 材质编辑
- `[ ]` 骨骼编辑
- `[ ]` IK/constraint 编辑
- `[ ]` Morph 编辑
- `[ ]` Label 编辑
- `[ ]` 刚体编辑
- `[ ]` Joint 编辑
- `[ ]` SoftBody 编辑
- `[ ]` UV 编辑器
- `[ ]` 顶点权重绘制
- `[ ]` bind pose 管理
- `[ ]` 模型验证器
- `[ ]` 模型导入/导出编辑流程
- `[ ]` PMD/PMX 写回兼容测试扩展

## 第七阶段：音频、视频与导出

- `[ ]` 跨平台音频播放
- `[ ]` 音频和 timeline 同步
- `[ ]` 音频波形或播放状态 UI
- `[ ]` 背景视频解码
- `[-]` 图片导出：已支持渲染中立 `RenderPngWriter` RGBA PNG 编码/写出，以及 CLI `--export-image <file.pmd|file.pmx|file.x|file.zmm|file.nma|file.pmm> <out.png> [width] [height] [--transparent] [--clear-color #RRGGBB[AA]]` 单帧导出、透明 framebuffer 请求和可配置清屏色；后续补编辑器导出 UI、帧号/相机覆盖参数、批量图片序列、资源路径诊断和视频导出联动
- `[ ]` 视频录制
- `[ ]` FFmpeg 或平台编码器适配
- `[ ]` 导出进度、取消和错误报告

## 第八阶段：插件宿主

- `[ ]` 插件基础接口与生命周期
- `[ ]` Decoder plugin
- `[ ]` Encoder plugin
- `[ ]` ModelIO plugin
- `[ ]` MotionIO plugin
- `[ ]` Effect plugin
- `[ ]` MeshSync plugin
- `[ ]` 插件发现、加载、卸载
- `[ ]` 插件 UI 和参数面板
- `[ ]` 插件错误隔离和诊断
- `[ ]` 跨平台 native/plugin 调用封装

## 第九阶段：工程资源、平台和发布

- `[ ]` 字体资源 bundle
- `[ ]` 翻译/本地化资源
- `[ ]` 图标和应用资源
- `[ ]` 最近文件、窗口布局、用户偏好持久化
- `[ ]` Windows 平台菜单、文件对话框和打包
- `[ ]` Linux 平台菜单、文件对话框和打包
- `[ ]` macOS 平台菜单、文件对话框、Info.plist 和打包
- `[ ]` 文件关联
- `[ ]` 崩溃/错误报告和日志系统
- `[ ]` 集成测试与跨平台冒烟测试

## 格式兼容补充

- `[ ]` binary DirectX `.x` 支持
- `[ ]` compressed DirectX `.x` 支持
- `[-]` PMM/ZMM/NMA 项目字段补全：effect 绑定、插件状态、UI 状态、资源路径细节
- `[-]` PMD/PMX/VMD/NMD 边界样本和兼容测试扩展
- `[ ]` nanoem reference fixture 对照测试集
